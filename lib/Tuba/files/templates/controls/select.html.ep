
% if (my $col = stash 'column') {
    % if (my $val = stash 'value') {
    % param $column->name => $val;
    % }
    %= select_field $column->name, $values, class => 'span8';
% } else {
    missing $column
% }

