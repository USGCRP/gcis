% layout 'default', namespaces => [qw/dcterms gcis fabio/];

## Filename
   dcterms:title "<%= $file->stringify %>";

% if ($file->location) {
   gcis:Location "<%= url_host($file->location) %>";
% }

## The SHA-1 hash pertaining to the file
% if ($file->identifier) {
   dcterms:identifier "<%= $file->sha1 %>";
% }

   a fabio:ComputerFile .
