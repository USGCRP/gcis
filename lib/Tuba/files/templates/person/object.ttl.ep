% layout "default";

<<%= current_resource %>>
   a prov:Person;
   dcterms:identifier "<%= $person->id %>";
   foaf:name "<%= $person->name %>";
   rdfs:label "<%= $person->name %>";
   gcis:url "<%= $person->url %>";
   a prov:agent ;
   gcis:url "<%= $person->url %>" .
% for my $c ($person->contributors) {
% my $org = $c->organization or next;
<<%= current_resource =%>> gcis:hasOrganization "<%= $org->identifier %>".
% }


