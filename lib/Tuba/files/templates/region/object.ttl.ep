% layout 'default', namespaces => [qw/prov dcterms xsd/];
%= filter_lines_with empty_predicate() => begin

 <<%= current_resource %>>
   a prov:Entity;
   dcterms:identifer "<%= $region->identifier %>";
   dcterms:title "<%= $region->title %>";
   dcterms:description "<%= $region->description %>"^^xsd:string.
% end
