% layout 'default';

% my @clone = @$results;
<center>
    Searched for <b><%= param 'q' %></b>.<br>
    <%= @clone %> result<%= @clone==1 ? '' : 's' %>
</center>
% while (my @next = splice(@clone, 0, 6)) {
    <div class='row'>
        %= include 'obj_thumbnails', objs => \@next;
    </div>
% }
